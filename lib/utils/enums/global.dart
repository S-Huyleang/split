/// This file contains all the enums used in the application
enum Status { pending, completing, completed }

/// This enum is used to set the priority of the task
enum Priorities { low, medium, high }

enum CurrentAction {save, edit, delete}